spring:
  h2:
    console:
      enabled: true

  datasource:
    url : jdbc:h2:mem:test

  jpa:
    properties:
      hibernate:
        # show_sql: true
        format_sql : true
        use_sql_comments : true

logging:
  level:
    org:
      hibernate:
        SQL : debug
#        type:
#          descriptor:
#            sql: trace
